//------------------------------Lifting_State_UP---------------------------------------------

// Lifting State Up in React JS means moving the from a child component to its parent so that multiple child components can share and synchronize the same data.


// When two or more components need to use the same data, you 'lift' that data (state) up to their common parent component and pass it down as props.


