//------------------------------------------DOM----------------------------------------------

// The DOM (Document Object Model) allows JavaScript to interact with, modify, and control HTML and CSS elements on the page, such as changing content, style, or structure dynamically.


// We can say that the way to access HTML through JavaScript is called DOM.
// These changes are called Dynamic changes or Dynamic manipulation.

//-------------------------------Window-Object-----------------------------------------------

// The Window Object in a brower is like the main container for everything in a webpage. It is browser's global object (NOT Part of JS) that displays the webpage (document,loctation JS Functions etc.)

// For Example :  console.log, alert etc. are part of window object.
window.console.log("HELLO MY WEB Fist Page of Web");
console.log("HELLO MY WEB Second Page of Web");

// How to check Window Object ?
// dir also use for all things show.

// console.log(window);
// console.dir(window);
console.log(window.document);
console.dir(window.document);


//------------------------------------------------------------------------------------------


// console.log(document.head);
// console.dir(document.head);
console.log(document.body);
console.dir(document.body);
console.dir(document.body.childNodes);





